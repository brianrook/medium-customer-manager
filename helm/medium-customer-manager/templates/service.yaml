apiVersion: v1
kind: Service
metadata:
  name: {{ include "medium-customer-manager.fullname" . }}
  labels:
    {{- include "medium-customer-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medium-customer-manager.selectorLabels" . | nindent 4 }}
